---
layout: layouts/base.njk
section: ons-verhaal
---

<!-- Start Page Banner -->
<div class="onze-ploegen-banner-area">
    <div class="container">
        <div class="page-banner-content">
            <h2>{{ title }}</h2>
        </div>
    </div>
</div>
<!-- End Page Banner -->

<!-- Start Expect Area -->
{% set ploegenlist = collections.ploegen %}
<div class="expect-area">
    <div>
        <div class="ploegen-diensten">
            {% for ploeg in ploegenlist %}
                <div class="expect-item" style="background-image: url('{{ ploeg.data.thumbnail }}'); background-repeat: no-repeat; background-position: center; background-size: cover;">
                    <h3>
                        {{ ploeg.data.title }}
                    </h3>
                    <p>{{ ploeg.data.content | safe }}</p>
                </div>
            {% endfor %}
        </div>
    </div>
</div>
<!-- End Expect Area -->
